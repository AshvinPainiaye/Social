{% extends 'base.html.twig' %}

{% block body %}
    <h1>AddFriend list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for addFriend in addFriends %}
            <tr>
                <td><a href="{{ path('addfriend_show', { 'id': addFriend.id }) }}">{{ addFriend.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('addfriend_show', { 'id': addFriend.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('addfriend_edit', { 'id': addFriend.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('addfriend_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
